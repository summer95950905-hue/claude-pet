# 狐獴桌宠 PRD v2.0（cc 修改）

> 本版本由 Claude Code 在 v1.0 基础上修改。
> 核心约束保持：**四只狐獴是产品的核心，不可砍**。
> 主要调整集中在：监听技术路线、客户端框架、美术资产策略、MVP 优先级排序、可观测的成功指标。
> 标注规则：每节末尾的 `[cc]` 段为本版相对 v1.0 的关键差异。

---

## 一、项目背景

Claude Code 在终端中执行任务时，用户经常需要反复查看状态：它是在思考、执行、等待确认，还是已经完成。与此同时，长时间等待 AI 工作也容易让用户错过审批节点、忘记喝水休息，任务完成后的反馈也偏工具化。

本产品希望通过一支「四只狐獴小队」，把 Claude Code 的工作状态转化为可感知、可陪伴、低打扰的桌面体验。它不是单纯通知工具，而是一个能帮用户「盯任务、守确认、管休息、庆完成」的桌面伙伴。

**[cc] 产品定位补充**：
四只狐獴不是装饰性的角色叙事，而是把「监控、确认、健康、反馈」四件事在认知上分离——用户余光扫过桌面就能定位「现在是谁在动」，从而知道当前发生了什么。这是四只小队作为产品核心的功能性理由，不只是 IP。

---

## 二、核心目标

1. **降低状态监控成本**——用户不必盯终端，也能知道 Claude Code 当前状态。
2. **避免错过确认节点**——等待输入 / 授权时桌宠及时提醒。
3. **建立健康工作节奏**——低打扰提醒喝水、起身、休息眼睛。
4. **增强任务完成反馈**——任务完成时正向庆祝。
5. **形成有记忆点的角色 IP**——四只各有职责、soul、动作、视觉。

**[cc] 新增可度量的成功指标**（用于判断 MVP 是否值得继续投入）：

- **D7 留存 ≥ 40%**：装了之后一周后还在每天开。
- **平均每日触发事件 ≥ 5**：监听是真的在工作，不是个静态摆件。
- **WaitingConfirm 平均响应时间下降 ≥ 30%**（用户自报对照）。
- 至少 5 位非作者用户主动分享截图 / 推荐——验证 IP 记忆点是否成立。

如果三个月内核心指标全部不达标，需要回到「四只小队」这个核心假设本身重新审视，而不是继续加功能。

---

## 三、技术路线

### 3.1 客户端形态

- **首发平台**：macOS（Apple Silicon 优先）
- **后续扩展**：Windows
- **技术栈**：**Electron**（MVP 阶段）
  - 选择理由：透明窗口、鼠标穿透、多屏吸附、置顶层级在 Electron 上文档与社区案例最成熟，迭代速度快
  - 后期若包体 / 性能成瓶颈，再评估迁 Tauri
- 桌宠窗口能力：
  - 透明背景、置顶显示、可拖拽、可吸附屏幕边缘
  - 可调整大小和透明度
  - 支持单只模式 / 小队模式（小队模式下四只成横向 / 纵向编队，间距可调）

**[cc] v1.0 列了「Tauri 或 Electron」让团队自己选，v2.0 直接定 Electron**——MVP 阶段不应在框架选型上消耗决策成本。

### 3.2 Claude Code 状态监听 ★ 重大调整

**v2.0 主路线：Claude Code Hooks + 本地事件总线**

用户继续使用原生 `claude` 命令，桌宠 app 不接管启动流程。监听通过 Claude Code 自带的 hooks 机制实现：

| Hook 事件             | 映射到的桌宠状态                  |
| --------------------- | --------------------------------- |
| `SessionStart`        | Idle → Running 切换的起点         |
| `UserPromptSubmit`    | Running（用户刚提交了 prompt）    |
| `PreToolUse`          | Running（执行工具调用中）         |
| `Notification`        | **WaitingConfirm**（核心触发点）  |
| `Stop` / `SubagentStop` | **Completed**                   |
| `SessionEnd`          | Idle                              |

实现方式：

1. 桌宠 app 安装时往 `~/.claude/settings.json` 注入若干 hook 配置
2. 每个 hook 是一段极轻的 shell（或调本地 CLI），把事件 POST 到桌宠 app 监听的 localhost socket（如 `127.0.0.1:<random_port>`，端口写到 app 配置）
3. 桌宠 app 收到事件后做状态机推导，分发给四只狐獴

辅助信号（不依赖 PTY）：

- **Stale 检测**：超过 N 秒没有任何 hook 事件，但 SessionStart 未配对 SessionEnd → Stale
- **Failed 检测**：`Stop` hook 携带的 exit / error 信息

**[cc] 放弃 v1.0 的 PTY 代理方案**，原因：

1. PTY 拦截 ANSI / TUI 输出极其脆弱，Claude Code 输出格式一变就崩
2. 强制用户从桌宠启动 `claude`，劝退资深用户
3. macOS 进程权限、签名、辅助功能授权链条长
4. Hooks 是 Claude Code 官方为这类集成设计的接口，稳定性、向后兼容性都有保证
5. Hooks 路线 1-2 天可跑通，PTY 路线评估 1-2 周仍不稳

**降级方案**：若用户用的是不支持 hooks 的旧版 Claude Code，提供「轮询模式」——读取 `~/.claude/projects/<project>/` 下最近会话文件的 mtime 做粗粒度状态推断。降级模式下 Knox 不可用（拿不到 confirm 事件），其他三只仍可工作。

### 3.3 事件分发机制

Scout 是事件总线的入口（不是 PTY 监听者，而是 hook 事件的接收 / 推导中心），将状态事件同步给其他三只：

- `WaitingConfirm` → 触发 Knox
- `Completed` → 触发 Pip
- 健康计时器到点 → 触发 Bubbles
- `Running / Thinking / Idle` → Scout 自身状态变化

**[cc] 新增**：四只狐獴之间的事件总线用 app 内部的 EventEmitter / Zustand store 即可，**不要**为此引入 IPC、Redis、消息队列等基础设施——四只都跑在同一个 Electron renderer 里。

### 3.4 多会话与多项目

- MVP：只跟踪「最近一次活跃的 Claude Code 会话」，多会话时以最后事件时间为准
- v2.1+：支持多会话并排显示，或在状态面板里切换

**[cc] 新增**：v1.0 没说多会话怎么办，会成为实现时的卡点。MVP 明确「最近一个胜出」，避免一开始就做会话路由。

---

## 四、功能描述

### 4.1 Scout 斥特 — 岗哨者

**职责**：监听 Claude Code 实时状态——思考、执行、空闲、等待确认、完成、异常。Scout 是整个小队的信息源头。

**Soul**：极度责任心，永远戒备。相信没有盲区，只有不够高的瞭望台。

**视觉设定**：像素风橙金色狐獴，奶油色脸腹，大眼睛，深棕眼圈，双手持望远镜，细长站姿，深色尾尖。

**行为**

| 状态           | 行为                       |
| -------------- | -------------------------- |
| Idle           | 短暂放松，但仍抬头观察     |
| Thinking       | 凝视远方，像在捕捉信号     |
| Running        | 站岗巡视，左右扫描         |
| WaitingConfirm | 转头通知 Knox              |
| Completed      | 向 Pip 发出完成信号        |
| Stale          | 踮脚或举望远镜加强观察     |
| Failed        | 耳朵压低，举异常小旗       |

**[cc] 实现优先级**：P0（MVP 必须）。Scout 是唯一直接消费 hook 事件的角色，先把它做完整。

### 4.2 Knox 克诺克斯 — 拥守者

**职责**：Claude Code 等待用户输入 / 审批 / 授权时第一个跳出来提醒。

**Soul**：处事谨慎、一板一眼。对任何未经授权的请求首先质疑，小本本上每一行都有迹可查。

**视觉设定**：像素风狐獴，红围巾，空白木牌，金色手铃，大眼睛，深棕眼圈，深色尾尖。

**行为**

| 状态         | 行为                       |
| ------------ | -------------------------- |
| Idle         | 抱着木牌待命，偶尔检查记录 |
| WaitingConfirm | 举牌、摇铃，提醒用户确认 |
| 等待过久     | 轻敲木牌或屏幕边缘         |
| 高风险请求   | 站到前方，严肃提醒         |
| 用户允许     | 盖章通过                   |
| 用户拒绝     | 合上本子，记录结果         |
| Failed       | 标记异常，表情严肃         |

**[cc] 实现优先级**：P0。Knox 是产品最有差异化的角色——「不会错过确认」是用户最痛的痛点。Knox 触发时建议附加：

- 一次温和的系统通知（macOS notification center）作为兜底
- 点 Knox 直接 focus 到对应的终端窗口（见 5.4）

「高风险请求识别」MVP 阶段不实现，拿到 `Notification` 事件就一律提醒，简单可靠。

### 4.3 Bubbles 吧布尔斯 — 小睡魔

**职责**：健康提醒。平时睡觉，计时器到点醒来提醒喝水、起身、休息眼睛。

**Soul**：近于懒散，深信休息是生产力。每次睁眼都像在拯救世界。

**视觉设定**：像素风狐獴，坐姿犯困，闭眼，深棕眼罩块，小圆嘴，双手抱浅蓝水杯，尾巴侧放。

**行为**

| 状态         | 行为                       |
| ------------ | -------------------------- |
| Idle         | 坐着或趴着睡觉，轻微呼吸   |
| 喝水时间     | 慢慢醒来，举起蓝色水杯     |
| 起身时间     | 伸懒腰，提醒站起来         |
| 护眼时间     | 揉眼睛，提示离开屏幕       |
| 用户确认完成 | 满意点头，继续睡           |
| 用户忽略太久 | 半梦半醒地盯着用户         |

**[cc] 实现优先级**：P1。Bubbles 不依赖 Claude Code 事件，纯本地计时器即可——是技术上最容易完成的角色，但功能价值相对低（市面上喝水提醒很多）。建议 MVP 里只做 idle + 喝水提醒两个状态，其余进 v2.1。

### 4.4 Pip 皮普 — 鸣花大师

**职责**：任务完成时撒花庆祝。平时闲逛 / 举旗加油，检测到完成信号冲出来庆祝。

**Soul**：生来的庆祝派。相信每一次完成都值得大肆庆祝，背包里永远备着彩带。

**视觉设定**：像素风狐獴，红色三角旗，棕色彩纸桶，彩色纸屑，开心站姿，大眼睛，深色尾尖。

**行为**

| 状态       | 行为                         |
| ---------- | ---------------------------- |
| Idle       | 闲逛，举旗，整理彩纸桶       |
| Running    | 在旁边轻轻加油               |
| Completed  | 撒花、跳舞、挥旗             |
| 连续完成   | 触发更热闹的连击庆祝         |
| Failed     | 收起彩纸，递出安慰反馈       |
| Review     | 探头看 Scout，确认是否该庆祝 |

**[cc] 实现优先级**：P0。Pip 是产品的「情绪锚点」，没有 Pip 就只剩工具感。

**[cc] 防骚扰约束**：Pip 的庆祝动效必须可一键禁用，且：

- 同一会话内 N 秒内多次 Completed 合并为一次庆祝（默认 30 秒）
- 用户处于全屏 / 勿扰模式时降级为静默撒花（无声音、无大动作）

---

## 五、设计规范

### 5.1 视觉风格

- 统一像素风桌宠
- 橙金色狐獴主体、奶油色脸腹、深棕眼圈与耳朵
- 大眼睛、高识别度
- 道具区分角色
- 适配 `192x208` 桌宠尺寸
- 避免复杂背景、文字、阴影、发光和过多粒子

**[cc] 美术资产分阶段策略**（关键调整）：

像素风 4 只 × 6-8 状态 × 多帧动画 = 数百帧资产，是 MVP 最容易爆掉的成本项。建议：

| 阶段     | 美术形态                           | 目的                       |
| -------- | ---------------------------------- | -------------------------- |
| MVP α     | 每只 3 帧 GIF / Lottie（idle / 主动作 / 提醒） | 验证交互与监听链路 |
| MVP β     | 每只 6 状态像素 sprite sheet         | 确立 IP 视觉识别         |
| v2.1+    | 完整动画集 + 表情变化               | 打磨陪伴感                 |

α 阶段允许用 AI 生成 + 人工修整产出 sprite，**不要让美术成为关键路径**。视觉风格规范（橙金色、像素、道具）从 α 就锁定，避免后期重做。

### 5.2 四只角色识别

| 角色    | 关键词         | 核心道具         |
| ------- | -------------- | ---------------- |
| Scout   | 警戒、站岗、信息源 | 望远镜           |
| Knox    | 审批、确认、守门   | 木牌、铃铛、红围巾 |
| Bubbles | 睡觉、健康、慢节奏 | 蓝色水杯         |
| Pip     | 庆祝、完成、正反馈 | 红旗、彩纸桶     |

### 5.3 动画原则

- Idle 动画低频、低干扰
- 关键状态动作明显
- 每只动作节奏不同：
  - Scout：稳定、警戒、扫描
  - Knox：克制、精准、提醒感强
  - Bubbles：慢、软、困
  - Pip：轻快、有弹性、庆祝感
- 不遮挡用户主要工作区域
- 支持降低动效模式
- **[cc] 新增**：「专注模式」一键收起所有动作，仅保留 Knox 在 WaitingConfirm 时的提醒——给深度工作场景兜底

### 5.4 交互规范

- 支持拖拽移动、吸附屏幕边缘
- 支持透明背景与置顶显示
- 支持调整大小和透明度
- 支持单只模式 / 小队模式
- 点击桌宠展开状态面板
- **[cc] 新增**：点击 Knox / Scout 可直接 focus 到对应 Claude Code 终端窗口（macOS 通过 AppleScript / Accessibility API 实现）——这是「桌宠 → 行动」的关键桥
- 右键菜单包含：暂停提醒 / 设置喝水间隔 / 设置休息间隔 / 关闭声音 / 调整动效强度 / 切换专注模式 / 退出应用

### 5.5 文案语气

文案短句、有角色感，但不过度卖萌。

- Scout：`我盯着呢，状态有变化。`
- Knox：`这个请求需要你确认。`
- Bubbles：`醒都醒了，喝口水吧。`
- Pip：`完成啦，彩带出场！`

### 5.6 隐私与安全

- 默认本地监听，不上传终端内容
- 不保存完整命令输出
- 不记录 token、密钥、路径等敏感信息
- 用户可关闭监听
- 首次启动明确说明监听范围
- 高风险命令识别只做提醒，不自动阻断，除非用户开启严格模式
- **[cc] 新增**：hook 注入到 `~/.claude/settings.json` 时必须显式提示用户、可一键卸载，不静默修改用户配置

---

## 六、MVP 范围（重新切分）

**[cc] 重大调整**：v1.0 的「首版」其实是 v1.0 完整版砍 20%。v2.0 把 MVP 切成 α / β 两阶段，每阶段都有可发布形态，四只狐獴贯穿始终。

### 6.1 MVP α —— 「跑通链路」（目标 1-2 周）

最小可用，给作者本人和 5-10 个早期用户使用：

1. Electron 透明置顶窗口
2. **四只狐獴**的 idle + 1 个主动作 GIF/Lottie（共 8 帧资产）
3. Claude Code hooks 注入与本地事件总线
4. Scout：Idle / Running / WaitingConfirm / Completed 四态切换
5. Knox：WaitingConfirm 时举牌 + 系统通知兜底
6. Pip：Completed 时撒花（30 秒去重）
7. Bubbles：固定间隔的喝水提醒
8. 右键菜单（暂停 / 退出 / 切换专注模式）
9. 点击 Knox 跳转终端窗口

### 6.2 MVP β —— 「立住 IP」（目标 +2-3 周）

在 α 验证通过后：

1. 完整像素 sprite，覆盖各角色全部状态
2. 小队 / 单只模式切换
3. Stale / Failed 状态完整支持
4. 健康提醒可配置（喝水 / 起身 / 护眼）
5. 状态面板（点击桌宠展开，显示当前会话信息）
6. 多动效强度档位
7. 降级模式（无 hooks 时的轮询）

### 6.3 暂不包含（沿用 v1.0）

- 复杂养成系统
- 云同步
- 皮肤商店
- 多终端无感监听
- 精细命令安全审计
- 多账号系统
- 多会话并排显示

**[cc] 新增暂不包含**：

- 高风险命令识别（MVP 一律走 `Notification` hook，足够）
- Pip 连击庆祝（α 阶段用基础撒花即可）
- 角色之间的对话 / 互动动画（先各司其职）

---

## 七、产品判断

这款产品的核心是：**把 Claude Code 的工作过程变成一支小队在陪你协作。**

Scout 负责看见状态，Knox 负责守住确认，Bubbles 负责照顾人，Pip 负责庆祝完成。用户不需要理解复杂系统，只要记住这四只狐獴正在帮自己盯着工作。

**[cc] 补充**：四只狐獴的角色分工同时也是技术架构——Scout 是事件入口、Knox 是中断管理、Bubbles 是定时器、Pip 是终态反馈。这种「角色即模块」的对应让产品叙事和代码结构天然对齐，是这款产品在工程上的优势。MVP 阶段每加一个功能都应能回答：「这是哪只狐獴的职责？」如果四只都不归，多半是不该做的功能。

---

## 八、v2.0 关键差异速查表

| 项                 | v1.0                       | v2.0（cc 修改）                              |
| ------------------ | -------------------------- | -------------------------------------------- |
| 监听技术           | PTY 代理拦 stdout/stderr   | **Claude Code Hooks + localhost 事件总线**   |
| 启动方式           | 用户从桌宠启动 `claude`    | 用户继续用原生 `claude`                      |
| 客户端框架         | Tauri 或 Electron          | **Electron**（明确选定）                     |
| 美术资产           | 一开始就要像素风全套       | **α 用 GIF/Lottie 跑通 → β 出像素全套**     |
| MVP 切分           | 单一「首版」               | **α / β 两阶段，各自可发布**                |
| 多会话             | 未提                       | MVP 只跟最近一个会话                         |
| 高风险识别         | MVP 包含                   | 移到 v2.1+                                   |
| 终端跳转           | 未提                       | 点 Knox / Scout 直达对应终端                 |
| 防骚扰             | 仅「暂停提醒」             | 新增专注模式 + 庆祝去重 + 全屏降级           |
| 成功指标           | 未定义                     | 明确 D7 留存 / 触发频率 / 响应时间下降       |
| 隐私               | 监听范围说明               | 新增 hook 注入透明化与一键卸载               |

四只狐獴的角色定位、Soul、视觉规范全部保留，核心叙事不变。
